export type Body = {
  name: string,
  color: string
};

export type Wins = {
  id: number,
  wins: number,
  time: number
};

export type ICar = {
  name: string,
  color: string,
  id: number
};

export type IdBody = {
  id: number,
};
